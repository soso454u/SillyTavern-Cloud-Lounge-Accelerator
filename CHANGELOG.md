# 更新记录

## 2.1.12 — 上游消息锚点与原生键盘布局

- 对照 SillyTavern 官方 `release` 与最新 `staging` 源码重做“显示更多”：在官方委托点击处理前记录第一条已显示消息的 DOM 坐标，旧消息插入后恢复同一消息的位置，不再依赖事件到达顺序或单次 `scrollHeight` 差值。
- 覆盖 SillyTavern `printMessages()` 中约 1.2 秒的完整媒体加载落底窗口；图片、视频或音频稍后触发 `scrollChatToBottom()` 时仍会恢复阅读锚点，用户开始下一次触摸、滚动或按键后立即释放。
- 移除插件的 `visualViewport` 监听、`#sheld` 高度覆写、键盘 inset CSS 与逐帧聊天落底，让输入区统一回归 SillyTavern 自带的 `interactive-widget=resizes-content`、`100dvh` 和 `ResizeObserver` 布局链。
- 不再区分 iOS 专用补丁；iOS/iPadOS、Android、Windows 触屏及桌面浏览器均由各自浏览器与 SillyTavern 原生逻辑处理键盘弹出和收回，避免重复上抬、巨大空隙、卡顿和偶发飞位。
- 版本统一升级到 2.1.12，Worker 使用新版本缓存名。

## 2.1.11 — 跨平台键盘收回稳定

- 键盘收回不再使用固定 450ms 后直接解除补偿；输入框失焦后继续跟随 `visualViewport` 和实际遮挡高度，可见区归零并稳定 120ms 后才清理键盘布局。
- 1.8 秒只作为浏览器丢失视口事件时的安全兜底，不再把某个系统的键盘动画时长当成全平台假设。
- 键盘弹出、收回与布局贴底使用同一条视口更新链，避免收回中途提前释放导致的二次上抬、空白和输入区跳位。
- 设备判定只依据触控能力或粗指针，不设 iOS 专用分支；新增 iOS、Android 和 Windows 触屏三类环境共用逻辑的回归测试。
- 版本统一升级到 2.1.11，Worker 使用新版本缓存名。

## 2.1.10 — 历史锚点与移动视口连续性

- `MORE_MESSAGES_LOADED` 会立即登记当前会话身份；即使它与 `CHAT_CHANGED` 的到达顺序变化，也不会把“显示更多”误判为首次进入聊天而重启落底校准。
- 键盘避让从独立移动 `#form_sheld` 改为只缩短 `#sheld` 实际被可见视口遮挡的部分，输入区与聊天区继续保持同一弹性布局。
- 浏览器已支持 `interactive-widget=resizes-content` 时自动检测原生缩放，键盘不再被计算两次，避免输入框飞到页面中间。
- 键盘开合时只在用户原本已接近聊天底部的情况下恢复底部锚点，阅读历史消息时不会被拉回最后一条。
- 新增历史事件乱序、原生视口已缩放、键盘关闭跟随和贴底恢复回归测试。
- 版本统一升级到 2.1.10，Worker 使用新版本缓存名。

## 2.1.9 — 移动输入区合成层跟随

- 修正 2.1.8 键盘避让造成的输入法工具条黑色底层：不再修改 `#sheld` 的 `height/max-height`，聊天页面继续铺在系统输入法工具条后方。
- 只对 `#form_sheld` 应用按帧合并的 `translate3d` 位移；键盘动画期间由合成层移动输入区，不再反复触发聊天列表布局与重排。
- 失焦后保留短暂的键盘跟随状态，让输入区随关闭动画平滑落回，不在键盘刚收起时突然跳到底部。
- 交互自愈改为静默执行：保留控制台诊断和高级状态记录，取消“已恢复输入焦点”“不可见弹窗”等 toastr 通知。
- 新增合成层位移、禁止整页高度覆写、键盘关闭跟随及无恢复弹窗回归测试。
- 版本统一升级到 2.1.9，Worker 使用新版本缓存名。

## 2.1.8 — 预设即时切换与移动键盘避让

- 预设开关适配扩展到非生成状态：触屏在 `pointerup`、鼠标在原生 `click` 时直接更新当前条目、开关图标与 token 占位，不再等待官方同步重建整个 PromptManager 列表。
- 普通开关不再安排完整列表重绘；生成期间仍在结束后统一刷新 token 状态，设置继续通过官方 `saveServiceSettings()` 保存，失败时仅回滚当前条目。
- 新增触屏 `visualViewport` 键盘避让：聚焦 `#send_textarea` 时按真实可视底边收缩 `#sheld`，使聊天输入区稳定停在软键盘上方；视口变化按帧合并，失焦或停用后完整清理。
- 交互自愈新增不可见但仍为 `open` 的 modal；持续隐藏超过保护期才关闭，正常可见弹窗和开场动画不会被处理。
- 补充 SillyTavern 旧式 `#shadow_popup`、角色与聊天选择遮罩的精确 hit-test；只有对应 popup 已不再渲染时才移除遮罩，不重放用户点击。
- 新增非生成即时切换、无完整列表重绘、键盘可视底边、隐藏 modal 与旧式遮罩回归测试。
- 版本统一升级到 2.1.8，Worker 使用新版本缓存名。

## 2.1.7 — 历史消息阅读位置稳定

- 首屏落底改为按聊天身份触发：实际进入或切换聊天时只执行一次，同一聊天内后续 `CHAT_CHANGED` 不再重启自动落底。
- 保留 SillyTavern 原生 “Show more messages” 的高度差定位；`MORE_MESSAGES_LOADED` 会取消尚未结束的首屏校准，加载更早消息后保持当前阅读位置。
- 首屏校准改用同步滚动，不再留下可能在取消之后执行的延迟动画帧；触摸、点击或滚轮输入都会立即停止校准。
- 新增同聊天历史更新、跨聊天切换、离开后重进及用户点击取消的回归测试。
- 版本统一升级到 2.1.7，Worker 使用新版本缓存名。

## 2.1.6 — 全平台交互自愈与移动端合成抽屉

- 新增 `InteractionRecoveryGuard`：Windows、macOS、Linux、Android、iOS 与 PWA 都会看护 SillyTavern popup 生命周期；只关闭超时 `closing` 或已确认没有活动 blocking handle 的孤儿 loader。
- 用户明确点按 `#send_textarea` 却未获得焦点时，检查 `disabled`、`readonly`、`inert`、可见性、pointer-events 和 hit-test；确认输入框可用且没有合法 modal 后才恢复焦点，不轮询、不抢其他编辑框。
- 残留 popup 挡住普通按钮时只解除 blocker，不重放点击；高级状态记录最近恢复原因、阻塞元素和累计次数，控制台额外记录浏览器与触控环境。
- `MobileInteractionGuard` 缩回触屏特例，只负责释放异常 Pointer Capture 和展开被隐藏的快捷回复执行窗；全平台 guard 只观察 popup 自身 `open/closing` 与子节点增删，不监听子树 class，也不再被两个 guard 重复观察。
- 1000px 以下粗指针设备的普通顶栏抽屉不再动画 `height: 0 → auto`，改为一次完成布局后使用 `transform + opacity`；普通触屏打开/关闭为 90/70ms，WebKit 为 80/60ms。
- 手机普通顶栏抽屉打开期间持续关闭实时 `backdrop-filter` 并禁用官方滚动条隐藏动画；左右大抽屉、桌面布局和“减少动态效果”保持原生行为。
- 版本统一升级到 2.1.6，Worker 使用新版本缓存名。

## 2.1.5 — 移动端抽屉帧稳定

- 针对 SillyTavern #5819：运行时精确识别官方仍未修复的三联顶栏 `:has()` 规则，并通过 CSSOM 原位包入 `min-width: 1001px`；手机不再执行该选择器，桌面行为保持不变。
- 补丁只匹配已知的 `z-index: 4005` 官方规则；上游套入桌面媒体查询后会自动 no-op，不修改 SillyTavern 磁盘文件，关闭“界面操作优化”时恢复原 CSSOM 规则。
- 移除抽屉高度动画期间的 `will-change: height`；继续只在动画窗口临时关闭毛玻璃，并保留桌面、普通触屏和 WebKit 的动画时长分档。
- 确认 `UiRenderOptimizer` 仍由 `InteractionOptimizer` 启动，`MobileInteractionGuard` 仍采用 body 结构观察 + dialog 局部属性观察，没有重新引入全局 attributes observer。
- 版本统一升级到 2.1.5，Worker 使用新版本缓存名。

## 2.1.4 — 全平台 UI 流畅模式

- 确认并保留 2.1.3 已接入的 `UiRenderOptimizer` 运行链路；修复的是其“仅触屏启用”的设备策略，而不是重复实例化模块。
- 新增 desktop profile：桌面顶栏抽屉打开/关闭调整为 160/130ms；普通触屏维持 150/110ms，iOS/iPadOS 维持 130/90ms。
- Popup 直接使用 SillyTavern 官方 `[opening]` / `[closing]` 属性分档缩短动画；动画期间才临时停用 backdrop blur，关闭阶段阻止重复交互。
- 拖动期间通过官方 `.ui-sortable-helper`、`.sortable-drag`、`.sortable-chosen` 状态只给当前面板降重，helper 消失后 CSS 自动恢复。
- 不增加全局 MutationObserver，不监听或阻止桌面 `pointerdown/click`，不调用 popup `close()`，不改写 drawer 或 sortable。
- 不启用全局 `translateZ(0)`、永久 `will-change`、全局关闭毛玻璃或实验性大型列表 `content-visibility`。
- 尊重系统“减少动态效果”：仅在 `prefers-reduced-motion: no-preference` 时调整抽屉与 Popup 动画。
- 高级状态可显示“桌面流畅”“触屏流畅”或“WebKit 流畅”；版本统一升级到 2.1.4。

## 2.1.3 — 有界配置备份与移动端顶栏快收

- Linux/macOS 与 Windows 安装器、服务端性能开关统一使用同一套配置安全写入器：内容无变化时不备份、不重写。
- 第一次实际修改前永久保存 `.cloud-lounge-accelerator/backups/config.original.yaml`，之后只滚动保留最近 3 份内容不同的修改前快照。
- 自动迁移根目录中旧 `config.yaml.backup-cloud-lounge-*`：最早一份作为基线，最近不同内容作为快照；复制成功后才删除旧式文件，其他来源的备份不处理。
- 配置写入后立即验证；写入或验证失败会自动恢复本次修改前内容。
- 新增触屏顶栏渲染优化：iOS/iPadOS 关闭动画缩短为 90ms，其他触屏为 110ms，只在动画期间临时停用抽屉模糊和添加 `will-change`。
- MobileInteractionGuard 不再观察整个页面的属性变化；全局只筛选弹窗结构变化，具体弹窗单独观察，并以 `requestAnimationFrame` 合并同帧检查。
- 桌面端、SillyTavern 原生按钮和抽屉状态管理保持不变；未默认启用大型角色列表的实验性 `content-visibility`。
- 版本统一升级到 2.1.3，Worker 使用新版本缓存名。

## 2.1.2 — iOS 主屏幕 Basic Auth 兼容模式

- 服务端健康接口只读返回 `basicAuthMode` 和基于 SillyTavern 核心程序文件生成的版本签名，不返回 Basic Auth 用户名、密码或其他凭据。
- iOS/iPadOS 主屏幕独立 Web App 与 Basic Auth 同时使用时，自动注销旧根作用域 Worker、清理本插件缓存并停止重新注册。
- iOS 兼容模式只停用 Service Worker 静态缓存；页面加载总开关、启动请求合并、聊天、正则和界面交互优化继续工作。
- 删除登录完成后的额外 `HEAD /` 版本探测，改用健康接口签名在 SillyTavern 核心文件更新后清理旧缓存。
- 高级信息新增“运行环境”，兼容模式显示为正常降级状态而不是插件故障。
- 新增 iPhone、iPadOS 桌面模式、非独立 Safari、macOS 和 Android 环境识别测试，以及 Basic Auth/签名服务端测试。
- 版本统一升级到 2.1.2，Worker 使用新版本缓存名。

## 2.1.1 — 后台欢迎页恢复提示瘦身

- README 与完整教程按 VPS/1Panel、Android/Termux、Mac/终端和 Windows/PowerShell 分别提供可直接复制的一键安装/更新命令，并使用各部署教程的实际默认路径。
- 删除会占满聊天区域的“酒馆界面已打开 / 后台读取最近聊天”占位层，启动恢复过程不再改变聊天区布局。
- 最近聊天仍使用原来的后台恢复和联网自动重试流程；正常加载只显示约 1.8 秒的顶部胶囊提示。
- 后台读取失败时也只显示短暂的小提示，不再留下纵向重试按钮或长时间空白占位。
- 版本统一升级到 2.1.1，Worker 使用新版本缓存名。

## 2.1.0 — 可回退的启动性能开关

- 设置面板新增互相独立的 `HTTP Keep-Alive` 与“角色卡懒加载”开关，直接显示网络异常、旧扩展兼容和角色模糊搜索退化等副作用。
- 开启前要求用户确认；每次实际修改先为根目录 `config.yaml` 创建带时间戳的备份，再以原子替换方式只修改对应布尔项。
- 配置保存后持续显示“待重启生效”；仅安装 UI 或配置只读时安全禁用/拒绝修改，不影响其他前端优化。
- Linux/macOS 安装器新增 `--keep-alive` 和 `--fast-start`，Windows 新增 `-KeepAlive` 和 `-FastStart`；普通安装仍不触碰这两项。
- `--fast-start` / `-FastStart` 只是 Keep-Alive 与角色懒加载的组合，安装输出和文档均明确列出副作用。
- 版本统一升级到 2.1.0，Worker 使用新版本缓存名。

## 2.0.9 — Safari 透明遮罩与触控失灵自愈

- 新增仅在触控设备启用的移动交互看门狗，不改写正常消息、复制、编辑或顶栏按钮的点击流程。
- SillyTavern 弹窗已进入 `closing` 却超过官方动画窗口仍停留在 Safari modal top layer 时，自动调用原生 `dialog.close()` 解除透明背景对整页的点击封锁。
- 记住加载器弹窗的生命周期；只在加载内容已被移除、且 SillyTavern 官方不再报告任何活动阻塞任务时清理孤儿遮罩，不会放行真正仍在运行的加载任务。
- 快捷回复执行窗最小化或被隐藏时，点到它的透明 modal 背景会自动展开控制窗口，让用户可以看到并停止任务；不会自动中断脚本。
- 触控结束时补充释放 Safari 残留的 Pointer Capture，并在页面从后台恢复时重新核对弹窗状态。
- 版本统一升级到 2.0.9，Worker 使用新版本缓存名。

## 2.0.8 — 弱网欢迎页、聊天落底与 Swipe 恢复

- 提前撤下启动遮罩后，空白聊天区会立即显示“后台读取最近聊天”占位；读取较久时出现手动重试，网络恢复也会自动调用 SillyTavern 官方欢迎页流程。
- 最近聊天接口在同一页面会采用“旧数据先显示、后台刷新”；返回欢迎页时不再为同一份列表重新阻塞界面，后台失败会保留上次成功结果。
- 删除 `SETTINGS_LOADED` 阶段主动并发请求头像、角色和背景的预取，避免弱网下三组额外请求与酒馆官方初始化争抢连接。
- 进入真实角色或群聊时，在确认最后一条已渲染后直接校准容器底部，不再受 SillyTavern 全局自动滚动偏好阻断；触碰、滚动或“显示更多”仍会立即取消。
- 同一布局稳定窗口调用 SillyTavern 官方 `refreshSwipeButtons()`，恢复已存在多 Swipe 消息却缺失的左右小三角，并继续尊重官方禁用、生成中隐藏和不可 Swipe 状态。
- 欢迎页不会被聊天落底逻辑拉走；版本统一升级到 2.0.8，Worker 使用新版本缓存名。

## 2.0.7 — Safari 聊天落底与移动端预设开关修复

- 切换进入聊天后，在首秒布局稳定期内通过 SillyTavern 官方 `scrollChatToBottom()` 校准最后一条，避免五条首屏在 Safari 中停在这批消息的第一条。
- 只有确认聊天最后一条已经渲染时才校准；用户触碰、按下、滚轮操作或官方 `MORE_MESSAGES_LOADED` 触发后立即取消，不影响“显示更多”的阅读位置。
- 生成期间为手机和触控笔增加预设开关 `pointerup` 快速通道，手指抬起即更新官方条目状态、图标与设置。
- 精确拦截同一开关随后产生的 Safari 幽灵 `click`，避免一次触摸切换两次；鼠标和非生成状态仍完全使用 SillyTavern 原生点击。
- 粗指针设备上的预设开关触控区域扩大到 `2.25rem`，不修改左侧原生拖拽手柄及 sortable 流程。
- 版本统一升级到 2.0.7，Worker 使用新版本缓存名。

## 2.0.6 — 服务端版本握手与旧 Worker 熔断

- 页面缓存启动前强制核对 UI 扩展与服务端插件版本，不再只检查健康接口是否可访问。
- 如果酒馆内自动更新了 UI、服务端目录仍停在旧版，立即注销本插件 Worker、清理本插件缓存并停止重新注册，避免旧版 Worker 在 HTTPS 域名下再次接管 Safari 导航。
- 高级信息会明确显示“服务端插件需更新”和服务端实际版本；服务端更新并重启 SillyTavern 后，刷新页面即可恢复安全的静态缓存。
- “修复插件”遇到版本不一致时安全跳过预热，不再向已注销的 Worker 发送消息。
- 版本统一升级到 2.0.6，Worker 使用新版本缓存名。

## 2.0.5 — 生成暂停与预设开关修复

- 流式生成期间暂停聊天代码块全量扫描，`MutationObserver` 只做常量级生成状态判断，正则模块也会直接忽略纯聊天 DOM 变化，避免重 HTML 持续变化时拖慢官方停止清理。
- 同时监听 `GENERATION_STARTED`、`GENERATION_STOPPED` 和 `GENERATION_ENDED`；停止事件发出后会等待官方 `isGenerating()` 真正解锁，再合并扫描一次代码块。
- 已排队的高亮任务在生成期间也会自动延后，不手动调用 `activateSendButtons()`，不修改官方 `body[data-generating]` 状态。
- 生成期间点击预设条目开关时，立即更新官方 `promptOrderEntry.enabled`、当前行图标和禁用样式，并调用官方 `saveServiceSettings()` 保存。
- 生成解锁后通过官方 `renderDebounced()` 合并为一次 Token 重算与列表刷新；不改写 `PromptManager.render`，不触碰 sortable 和拖拽逻辑。
- 生成期间切换只影响尚未发送的后续请求，不会篡改已经发送给模型的当前请求。
- 版本统一升级到 2.0.5，Worker 使用新版本缓存名。

## 2.0.4 — 原生拖拽与滚动热修

- 撤销预设、正则和世界书的自定义 PointerEvent 拖拽接管，不再禁用 SillyTavern 原生 sortable，电脑与手机继续使用官方排序和保存逻辑。
- 停用预设面板的延迟二次完整渲染，避免列表在拖动中被新 DOM 替换。
- 撤销“显示更多”点击和到顶滚动接管，删除额外的 `scrollTop` 锚点补偿，历史消息加载与阅读位置完全交还 SillyTavern。
- 停用重 HTML 消息的动态 `content-visibility` 占位，避免 iframe、剧情选项和大高度面板在滚动时因估算高度变化而跳动。
- 完整 HTML 源码不再要求 `language-html` 类名；只要包含完整页面结构即跳过 highlight.js，并同时兼容 `highlightElement`、`highlightAll` 和 `highlightBlock`。
- 正则精准刷新改为每批并行等待 `MESSAGE_UPDATED`；单条失败只记录状态，仅当局部刷新核心接口不可用时才回退完整聊天重载。
- 版本统一升级到 2.0.4，Worker 使用新版本缓存名并在激活时清理上一版资源。

## 2.0.3 — 拖拽修复与每页 5 条聊天

- 修复开启“界面操作优化”后预设、正则和世界书无法拖动：拖拽引擎现在会从嵌套手柄向上找到列表直属条目，不再把手柄本身误判为条目。
- 预设拖拽只接管 SillyTavern 标记为可拖动的提示词，固定条目继续保持原位。
- 移动端拖拽手柄单独使用拖动触控策略，避免纵向滚动手势抢走拖拽事件。
- 所有角色聊天首次统一只加载最后 5 条，不再使用 8～30 条自适应档位。
- 接近顶部或点击显示更多时，每页再补载 5 条；内部仍分小批执行并保持阅读位置。
- 版本统一升级到 2.0.3，并新增嵌套手柄直属条目解析和固定 5 条首屏测试。

## 2.0.2 — 正则最小影响刷新

- 编辑已有正则、单条开关和批量开关改用 SillyTavern 官方正则存储接口保存，不再先触发一次完整聊天重载。
- 正则状态从整份总签名改为逐条快照，可区分新增、删除、内容修改、顺序变化和作用范围变化。
- 修改查找式、替换内容或启用状态时，只刷新原始消息中可能匹配旧规则或新规则的消息；不匹配的重 HTML 界面保持现有 DOM。
- 只修改名称、编辑行为或仅影响提示词的正则时不刷新聊天；顺序、作用范围、宽泛表达式、宏查找式和无法编译的规则会安全刷新全部已显示消息。
- 增加前置规则产物追踪：如果前一条正则会生成后一条正则所需的标记，也会把前置规则命中的消息纳入刷新范围。
- 每条实际刷新的消息都会发送 SillyTavern 官方 `MESSAGE_UPDATED` 事件，帮助前端美化扩展及时重新处理目标消息。
- 手动“重新渲染当前聊天”继续保留全量模式；局部刷新失败时才回退官方完整聊天重载。
- 版本统一升级到 2.0.2，并新增无关重 HTML 保持、精准命中、无刷新改动、串联规则与安全回退测试。

## 2.0.1 — 重 HTML 懒渲染优化

- 新增完整 HTML 检测：识别 `DOCTYPE` / `html` 页面结构、页面样式或脚本，以及超长的 HTML 代码围栏。
- 检测到重 HTML 时，SillyTavern 官方聊天截断自动调整为电脑 5 条、手机或低性能设备 3 条；普通聊天仍保持原来的 8～30 条自适应范围。
- 聊天优化提前到应用就绪前监听，使聊天响应被正式渲染前即可应用重 HTML 截断。
- 重 HTML 聊天向上滚动时固定每批补载 2 条，已加载内容继续保留并使用屏幕外减负。
- 声明为 HTML 的超大完整网页源码跳过 `highlight.js` 拆词，保留纯文本、复制按钮和后续正则/HTML 美化流程。
- 屏幕外减负从固定照顾最后 5 条，改为优先照顾当前正在看的消息、上下相邻消息，以及正在生成时的最后一条。
- 版本统一升级到 2.0.1，并新增重 HTML 识别、3/5 条截断和代码高亮跳过测试。

## 2.0.0 — 自动化最终大版本

- 重构前端入口：`index.js` 从 1.5.0 的 1642 行全局接管器缩减为初始化编排，缓存、启动、聊天、正则、交互、修复、面板、适配器和工具各自独立。
- 普通面板只保留“页面加载加速”“聊天与重美化优化”“界面操作优化”三个开关，以及“重新渲染当前聊天”“修复插件”和只读高级信息。
- 删除 `?cla-safe=1` 普通入口、全局前端接管、强度档位、帧预算、批量/距离/冷却、第三方扩展缓存、暂停任务和完整重载等开发者控件。
- Service Worker 不再接管页面导航；页面版本检测移动到登录成功且 `APP_READY` 之后，由前端读取首页验证器并通过消息发送给 Worker。
- Worker 只缓存状态码严格等于 200、未重定向且不带 `WWW-Authenticate` / `Proxy-Authenticate` 的同源程序静态响应；静态请求显式使用同源凭据。
- 永久排除 API、用户数据与所有第三方扩展资源；扩展变更和版本签名变化继续清理程序缓存，激活时自动清理 1.x 遗留缓存。
- 收紧 1Panel 示例：静态响应改为浏览器私有缓存，认证响应不再因 `always` 获得缓存头，并按 Authorization/Cookie 区分。
- 保留并自动化启动请求预取/短期去重、自适应首屏消息、顶部旧消息补载、屏幕外渲染减负、代码延迟高亮与折叠、移动端滑动保护。
- 正则变更改为 dirty 合并机制：保存、开关、批量操作与拖拽完成后统一执行视口优先的分帧局部刷新，失败时才回退官方完整重载。
- 新增预设面板可见内容优先渲染、Token dry-run 空闲更新。
- 新增统一 PointerEvent 拖拽引擎和预设、正则、世界书适配器；移动期间只更新轻量影子与指示线，松手时只移动一次真实 DOM 并复用官方保存回调。
- 新增复杂列表抽屉的轻量合成动画和一键修复流程。
- 版本统一升级到 2.0.0，并按 SillyTavern 官方 `release` 提交 `8172dcd` 核对当前接口。

## 1.5.0 — 全局前端接管版

- 新增默认开启的“强力前端接管”总开关，以及平衡（8 ms）、强力（10 ms）、极致（12 ms）三档统一帧预算调度器；长任务出现后自动让出下一帧。
- 在官方 `SETTINGS_LOADED` 后安全释放 `app-init` loader，保留“后台仍在初始化”提示，并在 `APP_READY` 后自动撤下。
- 新增严格白名单的页面内请求复用：角色、头像、背景、扩展发现与静态模板仅在 URL、方法、请求体、请求头和请求选项完全一致时短期合并；写操作、超时或失败会失效并回退原生请求。
- 聊天读取结果不进入复用缓存；仅旁路分析最近消息的文本和富标记复杂度，在官方消费响应前后自动选择 8～30 条首屏截断值，停用接管时恢复原值。
- 正则编辑器通过脚本状态签名检测保存、开关和批量变更，连续操作合并成一次局部刷新；不阻止、不模拟官方保存逻辑。
- 官方“显示更多”按钮改走可恢复代理，顶部滚动可自动触发 1～10 条分帧补载，逐批使用可见消息锚点保持滚动位置；失败时直接调用官方原生接口。
- 聊天代码高亮采用局部代理：最近 3 条优先、旧代码进入视口后排队处理，并提供“旧代码不高亮”和“默认折叠”选项；聊天外代码始终走原生 `hljs`。
- 新增移动端纵向/横向手势状态机，减少垂直滚动后的左右 Swipe 误触。
- 新增 `?cla-safe=1` 本次安全模式、连续 3 次异常自动熔断、聊天空白 8 秒原生重载回退，以及暂停任务、恢复原生流程和完整重载入口。
- 禁用或删除扩展时恢复 `fetch`、聊天高亮、触摸/滚动代理、Observer、定时器和用户原聊天截断值。
- 版本统一升级为 1.5.0；新增请求白名单、自适应首屏、手势判断与调度优先级测试，测试总数增加到 18 项。

## 1.3.1

- “重新应用正则”改为原地刷新当前已显示消息，不再默认清空正则编译缓存、重新请求聊天或清空聊天 DOM。
- 局部刷新按“视口内 → 最近 5 条 → 其余已显示消息”的顺序执行，并根据 DOM 节点、HTML 长度、富组件数量和上一帧耗时自适应调整批量。
- 新刷新会取消旧刷新；新增进度、完成数量、耗时、失败提示和手动取消按钮。
- 原来的“清缓存并重载聊天”保留为需要二次确认的“完整重载（高级）”，仅用于局部刷新无效或正则状态异常。
- 新增“平滑加载更早 10 条”，使用 SillyTavern 官方 `showMoreMessages()` 小批量补入旧消息，并使用可见消息锚点保持滚动位置；不接管官方“显示更多”事件。
- 聊天诊断新增美化复杂度指标，综合当前 DOM 节点、HTML 体积以及 `details`、`table`、`pre`、`svg`、`iframe` 数量。
- `manifest.json`、`package.json`、前端核心与 Service Worker 版本统一升级为 1.3.1，并新增版本一致性、刷新优先级和自适应批量测试。

## 1.3.0

- 新增“重美化聊天模式”，一键联动官方聊天截断与屏幕外渲染减负，关闭时恢复开启前的设置。
- 每批显示档位改为 10、15、20、30、50 条，默认值从 50 降为 20 条。
- 普通渲染减负阈值从 80 降为 20 条；重美化模式下不再等待固定消息数量。
- “刷新正则渲染”更名为“重新应用正则”，明确仅用于正则修改后的旧消息刷新，不作为日常加速按钮。
- 新增旁路聊天加载诊断：聊天请求耗时、传输量、本次显示/总消息数、DOM 节点数、首批内容出现、前端处理估算、完整加载与最长主线程阻塞。
- 诊断只使用浏览器 Performance API、DOM 观察和 SillyTavern 官方事件；不包装全局 `fetch`，不接管滚动，不覆盖正则、Markdown 或代码高亮实现。

## 1.2.0

- 新增正式的纯 UI 模式：不安装服务端插件也能使用长聊天优化、正则刷新和性能指标，服务端缺失不再被当作插件故障。
- 新增“长聊天流畅模式”，使用 SillyTavern 官方 `chat_truncation`，支持每批 30、50、100 条，并在关闭或禁用扩展时恢复用户原值。
- 长聊天渲染减负改为消息达到 80 条后才启用，并保护最近 5 条消息、当前选中/编辑/聚焦消息。
- 扩展激活 Hook 立即返回；删除 jQuery 双重启动入口，服务端探测、Worker 注册和预热统一延后到 `APP_READY`。
- 自动预热默认关闭，同一版本只自动执行一次，并跳过后台页面、省流模式和 2G/慢速网络。
- 缓存统计改为展开面板或点击按钮时按需读取，不再在启动阶段重复枚举 Cache Storage。
- 不再监听整个 `document.body`；设置面板使用有限次数重试，禁用/删除时移除面板和所有观察器。

## 1.1.0

- 自动预热改为页面完成且浏览器空闲后执行，不再与 SillyTavern 首页初始化、字体和聊天渲染抢资源。
- 预热并发数从 6 降为 3，降低慢线路和低性能手机的瞬时负载。
- 新增正则编辑器、调试器、列表模板和主题对话框的空闲预热，改善首次打开的等待。
- 新增可选的“长聊天渲染减负”，跳过屏幕外复杂正则美化消息的布局与绘制。
- 新增“刷新正则渲染”，清理 SillyTavern 正则编译缓存并安全重载当前聊天。
- 完整教程新增无自有域名时使用 `sslip.io` / `nip.io` 和 1Panel 配置 HTTPS 的步骤。
- 补充正则保存后不生效、复杂美化 DOM 卡顿和聊天截断的排查说明。

## 1.0.3

- 修复操作按钮被窄面板压缩后中文逐字换行的问题。
- 三个操作按钮保持单行水平排列和水平文字；小屏宽度不足时可水平滚动，不再变成竖排文字。

## 1.0.2

- 移除设置标题前的云朵下载图标。
- 新增“删除本机加速”按钮，可一键注销当前设备的 Service Worker 并清除本插件的所有本地缓存。
- 补充“为什么酒馆内 UI 扩展安装不能代替首次服务端安装”的安全说明。

## 1.0.1

- 修复慢服务器首次预热容易超时的问题：预热现在优先复用浏览器已下载的 HTTP 缓存。
- 已命中的静态资源改为直接读取本地缓存，不再每次打开页面时同时发起数百个后台校验请求。
- 根页面签名未变时不再重写元数据，降低慢存储设备的启动延迟。
- 延长首次预热通信的最长等待时间，运行状态恢复后自动清除旧的超时提示。
- 新增 Android Termux、Windows、macOS、VPS/1Panel、受管云酒馆的支持表和安装指南。

## 1.0.0

- 首次公开版本。
